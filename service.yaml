apiVersion: v1
kind: Service
metadata:
  name: mynginx
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: mynginx
    app.kubernetes.io/instance: mynginx
    app.kubernetes.io/name: nginx-servier
  type: NodePort
